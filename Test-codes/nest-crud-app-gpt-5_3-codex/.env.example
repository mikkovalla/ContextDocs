# Option A: Single connection URL
DATABASE_URL=postgres://postgres:postgres@localhost:5432/nestdb

# Option B: Individual connection values (used if DATABASE_URL is omitted)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=nestdb

PORT=3000
